import sys
# sys.path returns a list of paths for python. On top it contains the base file (where the script is located) path.
print(sys.path)
#
# we can add new path using sys.path.append('/path/location')
"""sys.path.append('D:\Projects\pythonRev\Tricks')
for p in sys.path:
    print(p)"""
# appended path will be at the end of the list.
